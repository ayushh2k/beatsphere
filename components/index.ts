/**
 * Components master barrel export.
 * Import shared components from this entry point.
 *
 * @example
 * ```typescript
 * import { AlbumCard, SongCard } from '@/components/cards';
 * import { LocationConsentModal } from '@/components/modals';
 * import { DrawerContent } from '@/components/navigation';
 * ```
 */

// Re-export all type-based component groups
export * from './cards';
export * from './modals';
export * from './navigation';
